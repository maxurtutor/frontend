// @flow
'use strict';

export const saveProject = (): Promise<any> => new Promise(
        (resolve) => {
            setTimeout(() => {
                resolve();
            }, 1000)
        }
).then(() => {  }
);
